# Nuxt AI Chatbot Template

## Test

Make sure to install the dependencies:

```bash
pnpm install
```

Run package script:

```bash
pnpm package
```

> [!TIP]
> Inspect `.output/test/h3.ts` to see how to use in standalone node server.
